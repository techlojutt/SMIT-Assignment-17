

export default function SideCard() {
  return (
    <div className="newsletter">

        <h2 className="h2">Newsletter</h2>

        <div className="wrapper">

          <p>
            Subscribe to our newsletter to be among the first to keep up with the latest updates.
          </p>

          {/* <form action="#">
         { placeholder ?  <input type="email" name="email" placeholder="Email Address" required style={{border:'2px solid #EAEAEA'}}/> : null}

          {buttonLabel ?  <Button className = "btn button btn-primary   " title= "Subscribe" style = {{backgroundColor:'hsl(229, 76%, 66%)'}}/> : null}
          </form> */ }

        </div>

      </div>
  )
}
